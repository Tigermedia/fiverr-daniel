# Make Code Module 13 - Input Variables

Set these under **Input** in the Make Code module:

1. `photoLat` = `{{10.data.lat}}`
2. `photoLng` = `{{10.data.lon}}`
3. `projectLat` = `{{35.data.coordinates.lat}}`
4. `projectLng` = `{{35.data.coordinates.lon}}`
5. `photoUrl` = `{{10.data.uris[].uri}}`
6. `neighborhoodsCSV` = `{{30.data}}`
7. `keywordsCSV` = `{{31.data}}`

## Module settings (top of module)
- Language: `JavaScript`
- Input format: `Code editor`
